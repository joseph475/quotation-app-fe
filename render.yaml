services:
  - type: web
    name: quotation-app-fe
    env: static
    buildCommand: npm ci --include=dev && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_ENV
        value: production
